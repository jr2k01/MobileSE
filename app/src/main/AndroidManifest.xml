<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Nur lesen: CrewFit zaehlt keine Schritte selbst, sondern zeigt an, was
        andere Apps ohnehin in Health Connect eintragen. Geschrieben wird
        nichts, deshalb auch keine WRITE-Berechtigung.
    -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
        Bis Android 13 ist Health Connect eine eigene App. Ohne diesen Eintrag
        bliebe sie fuer CrewFit unsichtbar und die Anbindung meldete stets, es
        sei nichts vorhanden. Ab Android 14 gehoert Health Connect zum System.
    -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileSE">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".CrewLandingActivity" android:exported="false" />
        <activity android:name=".RegistrationActivity" android:exported="false" />
        <activity android:name=".CreateCrewActivity" android:exported="false" />
        <activity android:name=".MainHubActivity" android:exported="false" />
        <activity android:name=".CrewDetailsActivity" android:exported="false" />
        <activity android:name=".WorkoutHistoryActivity" android:exported="false" />
        <activity android:name=".WorkoutTrackingActivity" android:exported="false" />
        <activity android:name=".LeaderboardActivity" android:exported="false" />
        <activity android:name=".CrewChallengesActivity" android:exported="false" />
        <activity android:name=".MemberProfileActivity" android:exported="false" />
        <activity android:name=".WorkoutDetailActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!--
            Ziel des Links aus der Mail zum Zuruecksetzen des Passworts.
            Muss zu AppRepository.DEEPLINK_SCHEME/HOST passen und in Supabase
            unter den erlaubten Redirect-URLs stehen.

            Als einziger Weg von aussen in die App muss die Activity exportiert
            sein. Sie nimmt nichts entgegen ausser dem Link selbst, und ohne
            gueltige Sitzung darin laesst sich das Passwort nicht aendern.
        -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="crewfit" android:host="reset-password" />
            </intent-filter>
        </activity>

        <!--
            Health Connect verlangt, dass eine App erklaeren kann, wofuer sie
            die Gesundheitsdaten braucht. Aus der Systemoberflaeche heraus
            fuehrt ein Link hierher; fehlt der Bildschirm, laeuft er ins Leere.
        -->
        <activity
            android:name=".HealthPrivacyActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Ab Android 14 fragt das System ueber diesen Weg nach. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".HealthPrivacyActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.mobilese.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
