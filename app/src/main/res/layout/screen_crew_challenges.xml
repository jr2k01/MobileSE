<?xml version="1.0" encoding="utf-8"?>
<!--
    Eigener Bildschirm fuer die Crew-Challenges. Vorher standen sie oberhalb
    der Rangliste auf demselben Screen; dort waren sie nur zu finden, wenn man
    die Rangliste oeffnete, und schoben diese nach unten aus dem Blick.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <include
        android:id="@+id/topBar"
        layout="@layout/part_top_bar"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintBottom_toTopOf="@id/llChallengeButtons">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:clipToPadding="false">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <LinearLayout
                android:id="@+id/llChallengesContainer"
                style="@style/ContentColumn"
                android:paddingBottom="12dp" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!--
        Zwei Wege, eine Challenge zu beginnen: im eigenen Kreis oder gegen eine
        andere Crew. Untereinander und beide sichtbar, statt hinter einer
        Auswahl im selben Knopf - der Battle ist sonst nicht zu finden, und
        genau darum geht es bei einem Wettkampf: dass man ihn sucht.

        Der gewoehnliche Knopf steht unten und traegt die volle Farbe: er ist
        der haeufigere Fall.
    -->
    <LinearLayout
        android:id="@+id/llChallengeButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="@dimen/screen_padding_horizontal"
        android:paddingEnd="@dimen/screen_padding_horizontal"
        android:paddingBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLaunchChallenge"
            style="@style/Widget.CrewFit.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/add_challenge_btn"
            app:icon="@drawable/ic_add"
            app:iconGravity="textStart" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
